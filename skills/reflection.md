# Reflection Skill

Ability to review own actions, learn from outcomes, and adjust behavior.

## Example

```javascript
function reflect(actionResult) {
  const lessons = [];
  // analyze what worked, what didn't
  return lessons;
}
```