# Planning Skill

Ability to break down goals into actionable steps, prioritize tasks, and estimate effort.

## Example

```javascript
function planGoal(goal) {
  // break goal into subgoals
  const subgoals = [];
  // ...
  return subgoals;
}
```